/index.php       /            301

# The known bot-probe blocklist (soft-404 fix, see project/plan.md) lives in
# functions/_middleware.js instead of here — Cloudflare Pages' _redirects
# file does NOT support a 404 status code (only 200 and the 3xx redirect
# codes are honored; a `404` destination status is silently ignored and
# falls through to the next matching rule, confirmed live 2026-09-17 after
# ~20 min of a rule sitting here doing nothing). Functions also run before
# _redirects is even consulted, so this was doubly ineffective.
/*    /index.html   200
