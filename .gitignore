logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Personal notes on feedback from company owners, not project content
client_requests.txt

# Supabase CLI scratch
supabase/.temp/

# Generated per-invoice review sheets + downloaded product photos from the
# Rubies invoice import tool (scripts/import-invoice) — working data, not
# source, and can be large/numerous across runs.
imports/

# Windows shortcut(s) for double-click launchers (e.g. "Import Invoice.lnk")
# — bakes in this machine's absolute repo path, so it's local convenience,
# not something to version-control. import-invoice.cmd itself IS tracked
# (it's a portable relative-path script).
*.lnk
